import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
	container: {
		flex: 1,
		margin: 16,
		padding: 16,
		alignItems: 'stretch',
		justifyContent: 'flex-start'

	},
	input: {
		height: 40,
		marginBottom: 16
	}
});
